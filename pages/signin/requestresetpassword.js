import React from 'react'
import RequsetResetPassword from '../../components/RequsetResetPassword'

const requestresetpassword = () => {
    return (
        <RequsetResetPassword />
    )
}

export default requestresetpassword
